// This module has a dependency on Error.fn

#include "Error.fn"

class Result:
    any Data()
    Error Error(ErrLvl::Error, "Message", null)

    fn __Init(any Data, Error Err):
        this.Data = Data
        if Err != null:
            this.Error = Err
        esc
    esc
esc

fn Main():
    var Result t(use any(7), null)
    Std::Console::WriteLn(t.Data)
esc
