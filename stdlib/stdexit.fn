
pkg stdexit

export defn exit(x: int) {
    x # load to rax
    @Asmbl {
        mov rdi, rax;
        mov rax, 60;
        syscall;
    }
}

defn exit {
    @Asmbl {
        mov rax, 60;
        mov rdi, 0;
        syscall;
    }
}

defn main {
    exit
}

